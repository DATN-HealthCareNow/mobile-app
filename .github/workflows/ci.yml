name: CI - Build iOS (EAS)

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      profile:
        description: 'Build profile'
        required: true
        default: 'preview'
        type: choice
        options:
          - development
          - preview
          - production

env:
  NODE_VERSION: '20'

jobs:
  lint-and-typecheck:
    name: 🔍 Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout source code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🔍 Run ESLint
        run: npm run lint || true
        continue-on-error: true

      - name: 📝 TypeScript Check
        run: npx tsc --noEmit || true
        continue-on-error: true

  # build-ios:
  #   name: 🍎 Build iOS (EAS)
  #   runs-on: ubuntu-latest
  #   needs: lint-and-typecheck
  #   steps:
  #     - name: 📥 Checkout source code
  #       uses: actions/checkout@v4

  #     - name: 📦 Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'

  #     - name: 📥 Install dependencies
  #       run: npm ci

  #     - name: 🔧 Setup EAS CLI
  #       run: npm install -g eas-cli

  #     - name: 🔐 Authenticate with Expo
  #       run: npx eas-cli whoami || echo "Will use EXPO_TOKEN for auth"
  #       env:
  #         EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

  #     - name: 🍎 Build iOS IPA
  #       run: |
  #         npx eas-cli build \
  #           --platform ios \
  #           --profile ${{ github.event.inputs.profile || 'preview' }} \
  #           --non-interactive \
  #           --no-wait
  #       env:
  #         EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
